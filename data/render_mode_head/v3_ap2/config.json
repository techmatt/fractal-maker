{
  "model": "render_mode_head_v3",
  "arm": "ap2_selected",
  "target": "ordinal",
  "num_classes": 3,
  "loss": "CORN ordinal (K-1=2, K pinned=3), PER-SAMPLE WEIGHTED (model.corn_loss_weighted)",
  "geometry": "stretch",
  "label_unit": "render (image_id)",
  "augmentation": "geometric only (border_crop=0.05 per EDGE + axis_crop=0.0 per AXIS + h/v flip 0.5); NO color, NO jpeg jitter",
  "aug_rationale": "palette/mode/color IS the label (same as v1/head-v3)",
  "axis_crop": 0.0,
  "uniform_weights": false,
  "train_weight_by_kind": {
    "composite": {
      "train_rows": 348,
      "effective_weight": 342.17,
      "down_weighted_pct": 1.68
    },
    "direct": {
      "train_rows": 568,
      "effective_weight": 361.0,
      "down_weighted_pct": 36.44
    },
    "pure": {
      "train_rows": 571,
      "effective_weight": 558.83,
      "down_weighted_pct": 2.13
    }
  },
  "class_weighting": "none",
  "epochs": 40,
  "batch_size": 32,
  "backbone_lr": 0.0002,
  "head_lr": 0.001,
  "weight_decay": 0.05,
  "drop_rate": 0.2,
  "drop_path_rate": 0.1,
  "border_crop": 0.05,
  "num_workers": 4,
  "grad_clip": 1.0,
  "amp": "off",
  "selection": "max eval AP>=2 (marginal P>=2, 'not bad') on the deduplicated pooled eval side; full schedule, no early stop \u2014 v1's OWN objective, the (28b) fifth arm",
  "selection_metric": "ap_ge2",
  "selection_declared": "before training, in classifier/train_mining_head_v3.py",
  "init": "imagenet_backbone_fresh \u2014 NOT a finetune of v1 or v2",
  "split": "re-derived GLOBALLY over the pooled corpus (split_units.build_split, union-find over Julia-seed == parent point, family-stratified over UNITS); the per-batch stamps are INCONSISTENT and are recorded, not honored",
  "split_seed": 0,
  "eval_frac": 0.4,
  "row_weighting": "1/near_dup_group_size on the TRAIN side; the eval side keeps one row per group (data/render_mode_corpus/near_dup_groups_v1.json)",
  "batches": [
    "v1_sitting",
    "sheetB",
    "sheetC"
  ],
  "backbone": "mobilenetv4_conv_small.e2400_r224_in1k",
  "mean": [
    0.485,
    0.456,
    0.406
  ],
  "std": [
    0.229,
    0.224,
    0.225
  ],
  "interpolation": "bicubic",
  "input_size": [
    3,
    224,
    224
  ],
  "src_dims": [
    1280,
    720
  ],
  "target_dims": [
    384,
    224
  ],
  "black_thresh": 0.3,
  "gate": "marginal p_ge = cumprod(sigma); NEVER the CORN conditional",
  "seeds": [
    0,
    1,
    2,
    3,
    4
  ],
  "deviations_from_v1": [
    "DATA: pooled 3-batch corpus, not the lost dataset_v1/",
    "SPLIT: re-derived globally over the pooled locations",
    "WEIGHTS: 1/near_dup_group_size (v1 had no grouping)",
    "SELECTION: AP>=3, where v1 selected AP>=2",
    "MODES: every mode in the corpus; v1's trainer dropped three"
  ]
}