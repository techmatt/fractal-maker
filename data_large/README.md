# data_large/

Gitignored home for large, regeneratable datasets. Nothing in this directory is
tracked in git. Individual subdirs are safe to wipe if they can be regenerated.
Committed metadata/labels/checkpoints do NOT belong here — they stay under `data/`.
